services:
  - type: web
    name: stillwater-comshell
    runtime: python
    buildCommand: pip install -e . && pip install flask flask-cors gunicorn
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0