# Record Browser Field Card

`records.list` is for event files.

`sealed.list` is for event files that should not be visible but are.

`archive.index` is the older name. It still works because the older name is harder to convince.

Do not use file names as passwords. File names are bait. File names are often hungry.
