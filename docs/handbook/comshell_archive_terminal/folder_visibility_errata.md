# Folder Visibility Errata

Some folders may be mounted under more than one name.

Examples:

- `cards` and `index_cards`
- `sealed` and `archive`
- `tides` and part of `records`

Duplicate routes are not duplicates until they lead to the same room twice.
