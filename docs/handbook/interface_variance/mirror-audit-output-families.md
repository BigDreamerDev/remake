# Mirror Audit Output Families

Office of Inherited Silences / public training copy

This file is not authoritative. It exists to preserve terminal behaviour observed during low-clearance training sessions.

## Observation 01

Command: `mirror.audit`

The transcript was retained only to demonstrate that public commands may produce ceremonial output.

Do not compare this observation against mirrors after closing.

## Observation 02

Command: `mirror.audit`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not treat repeated phrasing as a key unless another document explicitly instructs you to do so.

## Observation 03

Command: `notice.roll`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 04

Command: `policy.random`

The transcript was retained only to demonstrate that public commands may produce ceremonial output.

Do not use this observation to predict restricted invocations.

## Observation 05

Command: `policy.random`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not treat repeated phrasing as a key unless another document explicitly instructs you to do so.

## Observation 06

Command: `cup.inspect`

The response appeared procedural but could not be matched to a live department. Training staff were advised not to infer hidden routing.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 07

Command: `waterline`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 08

Command: `mirror.audit`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not use this observation to predict restricted invocations.

## Observation 09

Command: `queue.status`

The transcript was retained only to demonstrate that public commands may produce ceremonial output.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 10

Command: `cup.inspect`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 11

Command: `queue.status`

The output was judged decorative, misleading, or redundant. No operational action was authorised.

Do not use this observation to predict restricted invocations.

## Observation 12

Command: `motd`

The transcript was retained only to demonstrate that public commands may produce ceremonial output.

Do not compare this observation against mirrors after closing.

## Observation 13

Command: `policy.random`

The wording changed after the room lights flickered. This is common in damp terminals and does not imply elevated clearance.

Do not use this observation to predict restricted invocations.

## Observation 14

Command: `cup.inspect`

The output was judged decorative, misleading, or redundant. No operational action was authorised.

Do not submit this note as Form S-44b; it will only return damp.

## Observation 15

Command: `stairwell.listen`

Output varied within acceptable institutional limits. The variance was not treated as an anomaly because the terminal remained polite.

Do not use this observation to predict restricted invocations.
